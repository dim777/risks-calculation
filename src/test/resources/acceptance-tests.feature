Feature: copy file between smb dirs
  Scenario: client makes call to POST /baeldung
    When the client calls /version
    Then the client receives status code of 200
    And the client receives server version hello