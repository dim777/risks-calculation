cql:
  keyspaceCreationQuery: "CREATE KEYSPACE IF NOT EXISTS testKeySpace WITH replication = { 'class': 'SimpleStrategy', 'replication_factor': '3' };"
  keyspaceActivateQuery: "USE testKeySpace;"

logging:
  level:
    org.hibernate: DEBUG